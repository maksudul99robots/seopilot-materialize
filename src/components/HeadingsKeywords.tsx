import { Chip } from "@mui/material"
import { Box } from "@mui/system"
import { CustomMadeChips } from "src/services/CustomMadeChips"

export const HeadingsKeywords = (props: any) => {
    return (
        <Box>
            <Box
                className="keyword-unused"
            >keywords for using</Box>
        </Box>
    )
}